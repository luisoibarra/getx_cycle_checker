name: getx_cycle_checker
description: Simple package for finding circular dependencies when using GetX package.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.15.1 <3.0.0'


dependencies:
  analyzer: ^3.2.0

dev_dependencies:
  lints: ^1.0.0
  test: ^1.20.1
